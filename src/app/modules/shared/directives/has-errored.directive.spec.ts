import { HasErroredDirective } from './has-errored.directive';

describe('HasErroredDirective', () => {
  it('should create an instance', () => {
    const directive = new HasErroredDirective();
    expect(directive).toBeTruthy();
  });
});
